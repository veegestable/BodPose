import { ScreenPlaceholder } from "@/components/ui/ScreenPlaceholder";

export default function PoseScreen() {
  return (
    <ScreenPlaceholder
      title="Pose Practice"
      description="Camera feed, skeleton overlay, and live alignment scoring will be implemented in the Pose units."
    />
  );
}
